services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4173:8080"
    command: ["nginx", "-g", "daemon off;"]
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://127.0.0.1:8080/ >/dev/null"]
      interval: 10s
      timeout: 5s
      retries: 5
