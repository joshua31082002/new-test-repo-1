package com.revolte.catalog.config;

import org.springframework.context.annotation.Configuration;

/** Catalog data is seeded by Flyway migrations so every environment uses the same database path. */
@Configuration
public class DataSeeder {
}
